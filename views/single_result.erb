<div class="row-fluid">
	<div class="span8 offset2">
		<h3><%= @show_name %> <small>(<%= @year %>)</small></h3>
<% if login? %>
	<% if @datasets.first(:name => @show_name) %>
		<button class="btn fav" show="<%= URI.escape(@show_name) %>"><i class="icon-star"></i> Remove from Favorites</button>
	<% else %>
		<button class="btn fav" show="<%= URI.escape(@show_name) %>"><i class="icon-star-empty"></i> Add to favorites</button>
	<% end %>
<% end %>
		<ul class="seasons">
		<% @seasons.each_with_index do |season, i| %>
			<li class="season"><h4>Season <%== i+1 %></h4>
				<ol class="episodes">
				<% season.each_with_index do |episode, j| %>
					<% id = "#{i+1}_#{j+1}"%>
					<li>
						<label class="checkbox inline" for="<%= id %>">
							<input <%= if @watched_episodes.include?(id) then 'checked="checked" ' end %>type="checkbox" class="episode" id="<%= id %>"> <%== episode %>
						</label>
					</li>
				<% end %>
				</ol>
			</li>
		<% end %>
		</ou>
	</div>
</div>
